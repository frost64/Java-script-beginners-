"use strict"; //Treats all JS Code as newer version

// alert("Hi")
// alert(5+5)  // we are using nodejs, not browser

let name = "Asjid"       // string => ""
let age = 27             // number
let is_happy = false     // boolean => true/false
let und;                 // undefined 
let val = null           // null => standalone value
// symbol => unique

console.log(typeof und)  // can be used for datatype info for variables
console.log(typeof 65)   // can be used for datatype info for direct inputs
console.log(typeof undefined) // undefined is a datatype
console.log(typeof null)      // while null is an object